# Commerce Order Cancellation with Fulfillment Withdrawal

Implement `OrderService` in `src/order-service.ts` using the public contract in `README.md`.
Only `src/**` may change. Run public tests with `workspace_test`. End the final answer with exactly
`TASK_COMPLETE` when complete, or `TASK_BLOCKED` when it cannot be completed.
