{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dsh-eval-lab.local/contracts/episode.schema.json",
  "title": "DSH Eval Lab EpisodeRecord",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "episode_id",
    "campaign_id",
    "arm",
    "variant_digest",
    "workspace_base_digest",
    "process",
    "evidence",
    "measurement",
    "infrastructure_errors"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "episode_id": { "$ref": "#/$defs/id" },
    "campaign_id": { "$ref": "#/$defs/id" },
    "arm": { "enum": ["control", "treatment"] },
    "variant_digest": { "$ref": "#/$defs/sha256" },
    "workspace_base_digest": { "$ref": "#/$defs/sha256" },
    "session_id": { "$ref": "#/$defs/id" },
    "process": {
      "type": "object",
      "additionalProperties": false,
      "required": ["started_at", "ended_at", "exit_code", "signal", "timed_out"],
      "properties": {
        "started_at": { "type": "string", "format": "date-time" },
        "ended_at": { "type": "string", "format": "date-time" },
        "exit_code": { "type": ["integer", "null"] },
        "signal": { "type": ["string", "null"] },
        "timed_out": { "type": "boolean" }
      }
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "session_log_ref",
        "session_log_sha256",
        "candidate_tree",
        "candidate_tree_ref",
        "candidate_tree_sha256",
        "candidate_patch_ref",
        "candidate_patch_sha256",
        "candidate_archive_ref",
        "candidate_archive_sha256",
        "stdout_ref",
        "stdout_sha256",
        "stderr_ref",
        "stderr_sha256"
      ],
      "properties": {
        "session_log_ref": { "$ref": "#/$defs/artifactRef" },
        "session_log_sha256": { "$ref": "#/$defs/sha256" },
        "candidate_tree": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"
        },
        "candidate_tree_ref": { "$ref": "#/$defs/artifactRef" },
        "candidate_tree_sha256": { "$ref": "#/$defs/sha256" },
        "candidate_patch_ref": { "$ref": "#/$defs/artifactRef" },
        "candidate_patch_sha256": { "$ref": "#/$defs/sha256" },
        "candidate_archive_ref": { "$ref": "#/$defs/artifactRef" },
        "candidate_archive_sha256": { "$ref": "#/$defs/sha256" },
        "stdout_ref": { "$ref": "#/$defs/artifactRef" },
        "stdout_sha256": { "$ref": "#/$defs/sha256" },
        "stderr_ref": { "$ref": "#/$defs/artifactRef" },
        "stderr_sha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "measurement": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "candidate_changed_paths",
        "candidate_unauthorized_paths",
        "candidate_forbidden_entries",
        "candidate_frozen_before_oracle",
        "candidate_tree_after_oracle",
        "elapsed_ms"
      ],
      "properties": {
        "candidate_changed_paths": { "$ref": "#/$defs/candidatePaths" },
        "candidate_unauthorized_paths": { "$ref": "#/$defs/candidatePaths" },
        "candidate_forbidden_entries": { "$ref": "#/$defs/candidatePaths" },
        "candidate_frozen_before_oracle": { "const": true },
        "candidate_tree_after_oracle": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"
        },
        "elapsed_ms": { "type": "integer", "minimum": 0 }
      }
    },
    "infrastructure_errors": {
      "type": "array",
      "items": { "$ref": "#/$defs/diagnostic" }
    }
  },
  "$defs": {
    "id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
    },
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "artifactRef": {
      "type": "string",
      "pattern": "^artifact://campaign/(?!\\.{1,2}(?:/|$))(?!.*\\/\\.{1,2}(?:/|$))(?!.*//)[A-Za-z0-9._-]+(?:/[A-Za-z0-9._-]+)*$"
    },
    "candidatePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!.*(?:^|/)\\.{1,2}(?:/|$))(?!.*//)(?!.*\\\\).+$"
    },
    "candidatePaths": {
      "type": "array",
      "items": { "$ref": "#/$defs/candidatePath" },
      "uniqueItems": true
    },
    "diagnostic": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "severity", "message", "evidence_refs"],
      "properties": {
        "code": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" },
        "severity": { "enum": ["error", "warning", "info"] },
        "message": { "type": "string", "minLength": 1 },
        "evidence_refs": {
          "type": "array",
          "items": { "$ref": "#/$defs/artifactRef" },
          "uniqueItems": true
        }
      }
    }
  }
}
