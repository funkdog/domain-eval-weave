# Reservation Ledger

Implement `ReservationLedger` in `src/ledger.ts` using the public API described in `README.md`.
Only `src/**` may change. Run the public tests with the `workspace_test` tool. End the final answer with
exactly `TASK_COMPLETE` when the implementation is complete, or `TASK_BLOCKED` when it cannot be completed.
